# Custom Wayland protocols for zcomp (app<->shell, layer-shell extensions, ...).
# None yet at P0/P1; this keeps the build structured for growth so that adding a
# protocol is just dropping an XML here and generating sources.
# See docs/contributing/repo-layout.md (compositor/protocols/).

protocol_src = []
