# The status bar: a wlr-layer-shell System UI client, installed as
# /usr/bin/zelto-bar and started by the initramfs after zcomp.
executable(
  'zelto-bar',
  files('main.c'),
  dependencies : zelto_dep,
  install : true,
)
