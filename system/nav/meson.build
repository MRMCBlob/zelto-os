# The bottom navigation bar: a wlr-layer-shell System UI client (Back/Home/
# Recents), installed as /usr/bin/zelto-nav and started by init like the bar.
executable(
  'zelto-nav',
  files('main.c'),
  dependencies : zelto_dep,
  install : true,
)
