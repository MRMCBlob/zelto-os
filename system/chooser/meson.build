# The app chooser (share sheet): a libzelto overlay layer-shell modal shown by
# zsysd to resolve an intent to a target app. Installed as /usr/bin/zelto-chooser.
executable(
  'zelto-chooser',
  files('main.c'),
  dependencies : zelto_dep,
  install : true,
)
