# The app launcher: a libzelto toplevel (app_id os.zelto.launcher) that fork/
# execs app binaries on tile tap. Installed as /usr/bin/zelto-launcher.
executable(
  'zelto-launcher',
  files('main.c'),
  dependencies : zelto_dep,
  install : true,
)
